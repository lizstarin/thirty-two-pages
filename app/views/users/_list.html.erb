<ul class="user-friends">
  <% users.each do |user| %>
    <li class="user-friend">
			<span class="user-thumb"><%= link_to image_tag user.avatar.url(:thumb), :url => user_url(user) %></span>
      <%#= link_to user.full_name, user_url(user) %>
    </li>
  <% end %>
</ul>